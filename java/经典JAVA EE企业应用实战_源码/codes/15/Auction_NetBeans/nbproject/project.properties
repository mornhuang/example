build.classes.excludes=**/*.java,**/*.form,**/.nbattrs
build.dir=build
build.generated.dir=${build.dir}/generated
client.module.uri=auction-war
client.urlPart=
debug.classpath=${javac.classpath}::${jar.content.additional}:${run.classpath}
display.browser=true
dist.dir=dist
dist.jar=${dist.dir}/${jar.name}
j2ee.clientName=${jar.name}
j2ee.deploy.on.save=false
j2ee.platform=1.5
j2ee.server.type=JBoss4
jar.compress=false
jar.content.additional=\
    ${reference.auction-war.dist-ear}:\
    ${reference.auction-ejb.dist-ear}
jar.name=Auction.ear
javac.debug=true
javac.deprecation=false
javac.source=1.5
javac.target=1.5
meta.inf=src/conf
no.dependencies=false
platform.active=default_platform
project.auction-ejb=auction-ejb
project.auction-war=auction-war
reference.auction-ejb.dist-ear=${project.auction-ejb}/dist/auction-ejb.jar
reference.auction-war.dist-ear=${project.auction-war}/dist/auction-war.war
resource.dir=setup
source.root=.
